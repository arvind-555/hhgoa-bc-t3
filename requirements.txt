# Piece 1 - face detection + encoding.
# dlib is the install hurdle on Windows. See README "Install" for the
# no-compiler path (dlib-bin) vs the build-it-yourself path.

numpy<2
Pillow
click
dlib-bin            # prebuilt dlib wheels (Win/mac/Linux, py3.8-3.12); avoids Visual Studio
face_recognition_models
face_recognition==1.3.0

# Piece 2 - Lenso.ai face search
requests

# Piece 2 (fallback) - Bing Images visual search when Lenso's paid tier is out
# of reach. After installing, run once:  playwright install chromium
playwright

# Piece 3 - Polygon Amoy testnet: wallet generation + RPC (brings in eth-account)
web3

pytest              # dev only
responses           # dev only - mocks HTTP in the piece-2 tests (never in src/)
